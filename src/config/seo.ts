/**
 * SEO config — re-exported from the centralized siteConfig.
 * Use siteConfig directly for full metadata. This file exists
 * for convenience and per-page metadata composition.
 */
export { siteConfig as seoConfig } from "@/config/site";
